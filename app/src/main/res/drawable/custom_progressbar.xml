<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#FFCEC2DC"
                android:centerColor="#FFCEC2DC"
                android:centerY="1.0"
                android:endColor="#FFCEC2DC"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#FF8C3AE9"
                    android:centerColor="#FF8C3AE9"
                    android:centerY="1.0"
                    android:endColor="#FFCEC2DC"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>